apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: map
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: kaljo14/my-map@sha256:be58b21dfe34e2b9daf528d47e7d6264c09baafbe1773b24875896b7b119d98a
          env:
            - name: ENABLE_AUTH
              value: "true"
            - name: PLACES_API_URL
              value: "http://places-scraper"
            - name: TILES_API_URL
              value: "http://tileserver"
            - name: KEYCLOAK_URL
              value: "https://keycloak.mustaci.com"
            - name: MARTIN_API_URL
              value: "http://martin"
          ports:
            - name: http
              containerPort: 80 # Adjust if your app listens on 3000 or 8080
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: map
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80 # Must match containerPort
      protocol: TCP
      name: http
  selector:
    app: frontend
