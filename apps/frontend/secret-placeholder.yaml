# This file is a PLACEHOLDER — do NOT apply it directly with real values committed.
# Fill in real values, then run:
#   kubectl apply -f apps/frontend/secret-placeholder.yaml
#
# Alternatively, create the secret via CLI:
#   kubectl create secret generic frontend-secret -n lonctus \
#     --from-literal=VITE_CLERK_PUBLISHABLE_KEY='pk_live_...'
#
# NOTE: VITE_ env vars are compile-time only. This secret is used during
# docker build (--build-arg), NOT as a runtime container env var.

apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: lonctus
type: Opaque
stringData:
  VITE_CLERK_PUBLISHABLE_KEY: "<CHANGE_ME>"
